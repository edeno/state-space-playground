"""State-space model playground.

A sandbox for experimenting with state-space models on neural / behavioral
data from the spatial bandit task. The ``data_loaders`` subpackage is
vendored from the ``continuum-swr-replay`` project and loads data via the
Spyglass pipeline. ``parameters`` and ``types`` are copied alongside it to
satisfy the loaders' internal imports.
"""
